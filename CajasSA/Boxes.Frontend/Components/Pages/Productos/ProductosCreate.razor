@page "/productos/create"

@if (loading)
{
    <Loading />
}
else
{
    <ProductosForm @ref="productForm" ProductDTO="productDTO" NonSelectedCategories="nonSelectedCategories" OnValidSubmit="CreateAsync" ReturnAction="Return" />
} 

﻿

